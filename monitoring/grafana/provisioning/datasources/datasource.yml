# Grafana 데이터소스 자동 프로비저닝: Prometheus를 기본 소스로 등록.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
